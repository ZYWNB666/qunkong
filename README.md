# QueenBee 运维管理平台

基于Vue 3 + Element Plus的现代化运维管理平台，提供脚本执行、执行历史和Agent管理功能。

## 功能特性

### 🚀 脚本执行
- 支持Shell和Python脚本
- 实时脚本编辑器
- 多服务器批量执行
- 参数化脚本支持
- 超时控制

### 📊 执行历史
- 任务执行记录查看
- 实时状态监控
- 详细执行结果展示
- 任务重试和停止
- 多维度筛选和搜索

### 🖥️ Agent管理
- Agent状态监控
- 系统信息查看
- 批量操作管理
- 版本更新控制
- 执行历史追踪

## 技术栈

- **前端框架**: Vue 3
- **UI组件库**: Element Plus
- **路由管理**: Vue Router 4
- **HTTP客户端**: Axios
- **构建工具**: Vite
- **代码编辑器**: Monaco Editor

## 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 项目结构

```
src/
├── api/                 # API接口
│   └── index.js
├── router/              # 路由配置
│   └── index.js
├── views/               # 页面组件
│   ├── ScriptExecution.vue    # 脚本执行页面
│   ├── ExecutionHistory.vue   # 执行历史页面
│   └── AgentManagement.vue    # Agent管理页面
├── App.vue              # 根组件
└── main.js              # 入口文件
```

## API接口

### 脚本执行
- `POST /api/execute` - 执行脚本
- `GET /api/tasks` - 获取任务列表
- `GET /api/tasks/:id` - 获取任务详情

### Agent管理
- `GET /api/agents` - 获取Agent列表
- `GET /api/servers` - 获取服务器列表
- `GET /api/agents/:id` - 获取Agent详情

## 配置说明

### 代理配置

开发环境下，API请求会自动代理到后端服务器：

```javascript
// vite.config.js
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:5000',
      changeOrigin: true
    }
  }
}
```

### 后端服务

确保后端服务运行在 `http://localhost:5000`，并提供相应的API接口。

## 开发指南

### 添加新页面

1. 在 `src/views/` 目录下创建Vue组件
2. 在 `src/router/index.js` 中添加路由配置
3. 在 `src/App.vue` 的侧边栏菜单中添加导航项

### 添加新API

1. 在 `src/api/index.js` 中定义API函数
2. 在组件中导入并使用

### 样式规范

- 使用Element Plus组件库
- 遵循BEM命名规范
- 响应式设计，支持移动端

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License
